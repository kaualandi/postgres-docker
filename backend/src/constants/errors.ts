export const WITHOUT_ROOT_PASS_BACKEND =
  'Esse comando é crítico, não é possível executa-lo pois uma senha root não foi definida no backend';

export const WITHOUT_ROOT_PASS_FRONTEND =
  'Esse comando é crítico, a senha root é necessária, pode informá-la clicando na chave ao lado do botão "Reset"';

export const INVALID_ROOT_PASS =
  'A senha root para executar comandos críticos (como esse) está incorreta';
